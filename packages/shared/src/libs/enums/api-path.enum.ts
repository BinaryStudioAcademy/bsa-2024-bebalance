const APIPath = {
	APP: "/app",
	ASSISTANT: "/assistant",
	AUTH: "/auth",
	CATEGORIES: "/categories",
	ONBOARDING: "/onboarding",
	QUIZ: "/quiz",
	TASKS: "/tasks",
	USERS: "/users",
} as const;

export { APIPath };
