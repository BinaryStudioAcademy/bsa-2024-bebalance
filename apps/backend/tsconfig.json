{
	"extends": "../../tsconfig.json",
	"include": ["**/*.ts", "lint-staged.config.js"],
	"exclude": ["build", "node_modules"],
	"compilerOptions": {
		"outDir": "build",
		"baseUrl": ".",
		"paths": {
			"~/*": ["./src/*"]
		}
	}
}
