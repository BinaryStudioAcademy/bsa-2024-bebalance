type AnimationConfig = {
	DAMPING: number;
	MASS: number;
	STIFFNESS: number;
};

const AnimationConfigValues: AnimationConfig = {
	DAMPING: 300,
	MASS: 0.5,
	STIFFNESS: 30,
};

export { AnimationConfigValues };
