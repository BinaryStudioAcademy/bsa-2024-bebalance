import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	btn: {
		alignItems: "center",
		backgroundColor: "blue",
		padding: 10,
	},
	label: {
		color: "white",
	},
});

export { styles };
