import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	btn: {
		padding: 10,
		backgroundColor: "blue",
		alignItems: "center",
	},
	label: {
		color: "white",
	},
});

export { styles };
