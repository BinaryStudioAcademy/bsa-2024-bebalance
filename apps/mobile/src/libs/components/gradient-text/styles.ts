import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	element: {
		alignItems: "center",
		backgroundColor: "transparent",
		flex: 1,
	},
	mask: {
		flex: 1,
		flexDirection: "row",
		height: "100%",
	},
});

export { styles };
