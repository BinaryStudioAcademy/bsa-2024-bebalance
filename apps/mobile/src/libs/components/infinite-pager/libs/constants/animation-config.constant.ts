type AnimationConfig = {
	damping: number;
	mass: number;
	stiffness: number;
};

const ANIMATION_CONFIG: AnimationConfig = {
	damping: 300,
	mass: 0.5,
	stiffness: 30,
};

export { ANIMATION_CONFIG };
